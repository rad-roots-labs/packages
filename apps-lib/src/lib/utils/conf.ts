import type { ClientWindow } from "$lib/types/conf";

export const wind: ClientWindow = {
    app: {
        layout: {
            mobile_base: {
                h: 600
            },
            mobile_y: {
                h: 768
            }
        }
    }
};